<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDrawer</item>
        <item name="colorPrimaryDark">@color/colorDrawer</item>
        <item name="colorAccent">@color/colorWhite</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavigationViewStyle">
        <item name="android:textColorPrimary">@color/colorWhite</item>
        <item name="android:iconPreview">@drawable/main_home_icon</item>
    </style>

    <style name="TextAppearanceMenuHeader">
        <item name="android:textColor">@color/colorDrawer</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="TextAppearanceItemWhite">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="TextAppearanceItemBlue">
        <item name="android:textColor">@color/colorBlue</item>
        <item name="android:textSize">24sp</item>
    </style>

</resources>
